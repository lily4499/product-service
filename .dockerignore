# Git metadata
.git
.gitignore

# Node dependencies (will be re-installed in image)
node_modules

# Local build outputs (image will build its own)
dist
build
coverage

# Logs & env
*.log
.env
.env.*

# Editor / IDE
.vscode
.idea

# CI / docs (not needed in image)
Jenkinsfile
*.md

